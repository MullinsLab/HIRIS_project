from django.core.management.base import BaseCommand, CommandError

# from import_wizard.utils.gff import GFFImporter

class InspectFile(BaseCommand):
    help = 'Inspects a file in preperation for importing '

    def add_arguments(self, parser):
        ''' Set the arguments for InspectFile '''
        parser.add_argument('poll_ids', nargs='+', type=int)

    def handle(self, *args, **options):
        ''' Do the work of inspecting a file '''

        # for import_file in options[]
